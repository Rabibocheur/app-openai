import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.assistance',
  appName: 'app-assistance',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
